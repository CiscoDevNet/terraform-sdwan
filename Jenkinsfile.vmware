pipeline {
  agent {
    kubernetes {
      yaml """
apiVersion: v1
kind: Pod
spec:
  containers:
  - name: terraform
    image: hashicorp/terraform:0.12.18
    command:
    - sleep
    args:
    - 99d
    tty: true
"""
    }
  }
  stages {
    stage('Terraform init') {
      steps {
        git 'https://github.com/CiscoDevNet/terraform-sdwan.git'
        container('terraform') {
          sh 'terraform init'
        }
      }
    }
  }
}
pipeline {
  agent {
    kubernetes {
    }
  }
  stages {
    stage('Clone repository') {
      steps {
        container('terraform') {
          checkout scm
        }
      }
    }
    stage('Terraform init') {
      steps {
        container('terraform') {
          sh 'terraform init'
        }
      }
    }
  }
}